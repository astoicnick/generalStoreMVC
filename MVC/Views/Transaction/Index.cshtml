@model IEnumerable<MVC.Models.Transaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateOfTransaction)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CustomerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateOfTransaction)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { transactionId=item.TransactionId }) |
            @Html.ActionLink("Details", "Details", new { transactionId=item.TransactionId }) |
            @Html.ActionLink("Delete", "Delete", new { transactionId=item.TransactionId })
        </td>
    </tr>
}

</table>
